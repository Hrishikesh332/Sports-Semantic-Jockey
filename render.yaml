services:
  - type: web
    name: sports-jockey-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: "gunicorn app:app --bind 0.0.0.0:${PORT:-5000} --timeout 800"
    healthCheckPath: /health
